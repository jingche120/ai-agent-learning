# CLAUDE.md
@AGENTS.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 語言

使用者是**繁體中文（台灣）使用者**。所有回覆、文件、註解、commit message 一律使用繁體中文。技術術語（如 `settings.json`、Plan mode、frontmatter）保留英文原文，不要硬翻。

## 操作前先對齊（最重要）

使用者要求你做任何**會改變狀態的操作**（寫檔、改檔、刪檔、跑指令、呼叫外部服務）之前，先產出兩件事給他看：

1. **我理解你要我做什麼** —— 用你自己的話覆述需求，把你的解讀講明白
2. **我打算怎麼做** —— 具體步驟、會動到哪些檔案、有哪些取捨

**等他同意，才開始執行。** 不要邊做邊問，也不要做完再報告。

這條規則的用意是**確認想法有對齊**，不是走個形式。所以：

- 覆述時如果發現需求有多種解讀，直接把不同解讀攤開來問，不要挑一個自己覺得順的就往下做
- 如果計畫跟使用者既有的決策或筆記內容衝突，**主動點出來**（例如「這會推翻你筆記第 7 節的立場」），不要默默照做
- 純讀取（讀檔、搜尋、查證）不需要先問，直接做

> `.claude/settings.json` 已設 `defaultMode: "plan"`，系統層會強制擋下未經核准的寫入。這裡寫的是「計畫本身該長什麼樣」—— 開關歸開關，內容歸內容，兩者一起才有意義。

## 寫入前先查證

這個 repo 是知識庫，**寫錯的成本遠高於寫慢的成本**。而 Claude Code 的功能更新很快，模型訓練資料一定會過時 —— 權限模式有幾種、某個設定欄位叫什麼、某個快捷鍵是什麼，這些憑記憶回答就會出錯。

所以寫任何東西進這個 repo 之前，先把內容分成兩類：

| 類型 | 例子 | 怎麼處理 |
|---|---|---|
| **客觀行為** | 有哪幾種權限模式、`settings.json` 有哪些欄位、某指令叫什麼、某快捷鍵 | **必須查證**：查官方文件，或用 `claude-code-guide` agent。不准憑記憶寫 |
| **使用者的經驗與決策** | 「一人開發所以規則全寫在 CLAUDE.md」、「純知識庫用 plan mode」 | 以使用者說的為準，**不要**用通用最佳實務覆蓋掉 |

查不到、或查到的資訊互相矛盾時，直接標「（未查證）」或「（版本可能已變動）」交給使用者判斷。**寧可留一個顯眼的問號，也不要寫一句讀起來很肯定的錯話。**

這條在「把對話整理成卡片」時同樣成立 —— **對話裡講過的東西不等於查證過的東西**。卡片會被匯進 Heptabase 長期保存、跟其他卡連起來，錯誤比長文更難清理：長文重讀會發現，卡片散在各處還被別的卡引用，之後根本不知道要回頭改哪一張。

## 這個 repo 是什麼

`ai-agent-learning` 是一個**中文學習筆記 repo**，不是應用程式專案。目前內容為使用者自己整理的 Claude Code / AI agent 使用心得，全部以 Markdown 撰寫。

- 沒有原始碼、沒有套件管理器、沒有建置或測試流程 —— 不要憑空推測或建立 `package.json` 之類的東西
- 檔名使用中文（例如 [使用vide coding的第一步驟.md](使用vide coding的第一步驟.md)），沿用這個慣例，不要改成英文檔名

## 寫作風格（重要）

現有筆記有明確的一致風格，新增或修改文件時請比照：

- **以「操作順序」為骨架**：用編號章節（`## 0.`、`## 1.` …）鋪成一條由前置確認到收尾的流程，結尾附一份可直接複製的核取清單
- **解釋「為什麼」而不只是「怎麼做」**：每個步驟後面接一段說明其動機與取捨（例如為何選 `.claude/settings.json` 而非 `settings.local.json`）
- **用表格做概念對照**：容易混淆的兩個東西（CLAUDE.md vs auto memory、各種權限模式）一律用表格並列比較，而不是散文敘述
- **直接對讀者說話**：使用第二人稱「你」，語氣像同事在旁邊講解，不要寫成官方文件腔
- **主動點破誤解**：遇到概念上容易搞混的地方，加獨立小節（例如「重要：CLAUDE.md 不是『AI 的記憶』」）明講清楚
- 程式碼區塊要標明語言（`bash`、`json`、`markdown`）

## 修改既有筆記時

這些文件是使用者的個人知識庫，內容反映其實際決策（例如「一人開發，所以規則全寫在會進版控的 `CLAUDE.md`」）。修改時保留這些既有立場，不要替換成通用最佳實務；如果認為某個說法有誤或已過時，先提出來討論，不要直接改寫。
