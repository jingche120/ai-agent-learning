# AGENTS.md

## 專案定位

`ai-agent-learning` 是使用繁體中文整理的 AI agent 學習專案，目前以 Markdown 學習筆記、實作紀錄與驗證結果為主。

本專案的第一目標是讓使用者真正學會並能獨立實作、驗證與除錯。完成核心技術與整合專案後，才開始練習教學與顧問能力。

這不是應用程式專案。除非使用者明確決定加入程式碼或工具鏈，否則不要建立 `package.json`、套件設定、建置流程或其他應用程式結構。

## 語言

- 所有回覆、文件、註解與建議的 commit message 使用繁體中文（台灣）。
- 技術名稱、指令、設定欄位與專有名詞保留英文，例如 `AGENTS.md`、frontmatter、Plan mode。

## 學習與操作原則

- 一次只引導使用者完成一個步驟。
- 每個步驟說明目的、執行方式與驗證方法。
- 專案建立、檔案編輯與 Git 操作原則上由使用者親手執行。
- 除非使用者明確要求 Codex 代為操作，否則不要建立、修改或刪除檔案，也不要執行會改變狀態的指令。
- 使用者明確要求代為操作時，只修改其指定範圍；不要擴張成其他變更。
- 唯讀檢查可以直接執行，例如讀取檔案、搜尋內容、查看 Git 狀態與檢查差異。
- 發現需求有多種合理解讀，且不同解讀會明顯改變結果時，先向使用者釐清。

## 寫入前先查證

- 容易變動的 Claude Code、ChatGPT／Codex 或其他 AI agent 客觀功能，必須先查證，不要只依賴模型記憶。
- 優先使用目前環境提供的官方文件工具或適用 Skill；若不可用，再查閱該產品的第一方官方文件。
- 使用者的經驗、偏好與專案決策以使用者說法為準，不要用通用最佳實務擅自覆蓋。
- 查不到或來源互相矛盾時，明確標示「未查證」或「版本可能已變動」。
- 如果查證結果與既有筆記不同，先說明差異並與使用者討論，不要直接改寫其原有立場。

## 文件規範

- 使用 Markdown。
- 檔名優先使用中文，並沿用 repository 的既有命名方式。
- Codex 產生的長篇學習筆記放在 repository 根目錄的 `new-note/`。
- Codex 產生的卡片盒筆記放在 repository 根目錄的 `card/`。
- `new-note/` 是筆記輸出目錄；不要與 Skill 目錄 `.agents/skills/new-note/` 混淆。
- `card/` 是匯入 Heptabase 前的暫存區，依 `.gitignore` 不進版控；`new-note/` 的長篇筆記則進入版控。
- 以操作順序組織教學筆記，使用編號章節從前置確認寫到驗收。
- 每個步驟除了說明怎麼做，也要解釋為什麼這樣做及其取捨。
- 容易混淆的概念優先使用表格比較。
- 使用第二人稱「你」，語氣像同事在旁邊說明，避免官方文件腔。
- 容易產生誤解的概念應建立獨立小節主動說明。
- 程式碼區塊標示語言，例如 `bash`、`json`、`markdown`。
- 適合實作的長篇筆記在結尾加入可直接使用的核取清單。
- 修改既有筆記時保留使用者的原始決策；若認為內容錯誤或過時，先提出查證結果。

## 專案 Skills

- Codex 的 repository Skills 位於 `.agents/skills/`。
- 對話整理成卡片盒筆記時使用 `$card`，產物放在 `card/`。
- 新增或大幅改寫學習筆記時使用 `$new-note`，產物放在 `new-note/`。
- Skill 的具體流程以各自的 `SKILL.md` 為準。

## 驗證方式

- 修改後重新讀取相關段落，確認內容確實寫入。
- 檢查 Markdown 標題、列表、程式碼區塊與相對連結。
- 查看 `git status`，確認只出現預期的變更。
- repository 尚無程式碼、套件管理器或測試流程時，不要虛構建置與測試結果。
- 驗證失敗時先說明實際錯誤，再決定下一步；不要宣稱未執行的檢查已通過。

## Git 規則

- `git add`、`git commit`、設定 remote 與 `git push` 原則上由使用者親手執行。
- 除非使用者明確要求，Codex 不執行上述操作。
- 查看 `git status`、`git diff`、`git log` 與 remote 設定屬於唯讀檢查，可以直接執行。
- 不要使用破壞性 Git 指令，除非使用者明確指定並確認目標與影響。

## 與 CLAUDE.md 對齊

- `AGENTS.md` 與 `CLAUDE.md` 應維持相同的專案目標、學習原則、文件風格與 Git 權責。
- Claude 專用設定只放在 `CLAUDE.md` 或 `.claude/`；Codex 專用設定只放在 `AGENTS.md` 或 `.agents/`。
- 共用規則改變時，提醒使用者檢查另一份規則檔是否需要同步；除非使用者明確要求，不要自動修改另一份檔案。
